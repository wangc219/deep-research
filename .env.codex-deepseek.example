# DEPRECATED compatibility stub.
# Put DeepSeek MODEL / BASE_URL / API Key in the unified `.env` instead
# (see the "DeepSeek" section in `.env.example`).
#
# If you still need a separate overlay for experiments:
#   EQUIPMENT_DR_DEEPSEEK_ENV_FILE=/path/to/overlay
# otherwise start-local reads DeepSeek settings from `.env` only.
