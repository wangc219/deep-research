# 2026-08-05 工作区存档备注

本次存档记录 Query 驱动的装备需求挖掘与交付链路改造：

- 将流程族识别从本地关键词/装备名词穷举，调整为由 Codex CLI 基于整卡语义合同判断。
- 将主装备身份、平台与载荷边界、释放域、目标和直接战果等约束前置到 S5/S6 首稿，并在生成阶段自检；不再用本地标题或装备词表覆盖 Codex 锁定的身份。
- S6 采用“组合规划 → 5–7 张独立装备能力画像并行生成 → 组合装配”的模式，以支持每项装备的独立能力画像。
- 增加字符串 `"true"` 到 JSON 布尔值 `true` 的兼容解析，并调整运行并发、超时和质量模式的配置。
- 同步归档 Query Library、报告质量门、审计策略、能力画像模块、前后端接入、测试、运行脚本、知识索引和阶段性对比文档。

交付状态：r17 已产出于
`outputs/runs/real-swarm-quality-v1-cross-island-20260805-r17-parallel-s6-title-locked`。
该轮 `audit_status=limited`：报告质量门未完全通过，主要缺口为逐装备完整能力画像未全部写入 Markdown，且跨章节长句复用偏多。按当前决策，r18 暂不执行。

本提交特意不纳入 `outputs/runtime/codex-homes/**`：其中包含 Codex 运行态数据库、WAL/SHM、技能缓存及可能的认证状态，不属于可复现的源码或研究交付物。
